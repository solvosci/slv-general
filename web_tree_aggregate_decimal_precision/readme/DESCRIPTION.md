By default, summary totals don't follow the source field’s digits configuration and always will show 2 decimals digits.
This module allows to set the decimal precision of the aggregate values in tree views.
